#ifndef UTILS_H_
#define UTILS_H_

#include <string>
#include <vector>

// Parses the command line arguments into a vector of UTF-8 strings.
std::vector<std::string> GetCommandLineArguments();

#endif  // UTILS_H_
